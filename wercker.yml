box: wercker/golang

build:
    steps:
        - script:
            name: coveralls
            code: |-
                go get github.com/mattn/goveralls
                goveralls -repotoken COVERALLS_REPO_TOKEN

deploy:
    steps:
        - heroku-deploy:
            key-name: HEROKU_DEPLOY_KEY
